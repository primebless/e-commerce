import { Paper, Typography } from '@mui/material';

const ContactPage = () => (
  <Paper sx={{ p: 3 }}>
    <Typography variant="h4" sx={{ mb: 2 }}>Contact</Typography>
    <Typography>Email: support@primestore.dev</Typography>
  </Paper>
);

export default ContactPage;
