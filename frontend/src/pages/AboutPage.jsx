import { Paper, Typography } from '@mui/material';

const AboutPage = () => (
  <Paper sx={{ p: 3 }}>
    <Typography variant="h4" sx={{ mb: 2 }}>About Prime Store</Typography>
    <Typography>Prime Store is a modern commerce platform focused on fast checkout and reliable delivery.</Typography>
  </Paper>
);

export default AboutPage;
